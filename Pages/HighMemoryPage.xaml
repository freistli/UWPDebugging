<Page
    x:Class="UWPDebugging.Pages.HighMemoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPDebugging.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Style TargetType="Rectangle">
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
    </Page.Resources>

    <!--Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="120"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="120"/>
        </Grid.ColumnDefinitions>
        <Rectangle x:Name="RedR"  Fill="Red" Grid.Row="0" />
        <Rectangle x:Name="BlueR" Fill="Blue" Grid.Row="1" />
        <Rectangle x:Name="GreeR" Fill="Green" Grid.Column="1" />
        <Rectangle x:Name="OrangeR" Fill="Orange" Grid.Row="1" Grid.Column="1" />
    </-->
    <StackPanel x:Name="myStack" Orientation="Vertical">
        <Button Content="High Memory" Click="Button_Click">
        </Button>        
    </StackPanel>
</Page>
